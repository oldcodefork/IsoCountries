A simple class for returning ISO3 country codes and names. Optionally filter countries out of the list and/or move countries to the top of the list. Assumed it'll be used in something like an HTML select element.

Features:

* Get all country codes and names
* Filter out specific countries from returned list
* Move specific countries to top of returned list
* Return list of EU member states
* Return list by continent
